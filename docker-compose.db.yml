services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: soberstreak
      POSTGRES_DB: soberstreak
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
